<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="@Url.Action("Index", "Home")">Angora</a>
        </div>

        <div class="collapse navbar-collapse" id="navbar-collapse">

            @if (Request.IsAuthenticated)
            {
                <ul class="nav navbar-nav">
                    <li><a href="#">Events</a></li>
                    <li><a href="#">Friends</a></li>
                    <li><a href="#">Other Things?</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class=" dropdown-toggle" data-toggle="dropdown">@User.Identity.Name<b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li><a href="@Url.Action("Index", "Account")">Manage Account</a></li>
                            <li><a href="#">Privacy?</a></li>
                            <li><a href="#">Something else here</a></li>
                        </ul>
                    </li>
                    <li>
                        <a href="@Url.Action("Logout", "Account")">Logout</a>
                    </li>
                </ul>
            }
            else
            {
                using (Html.BeginForm("ExternalLogin", "Account", new { ReturnUrl = Url.Action("Index", "Home") }))
                {
                    @Html.AntiForgeryToken()
                    <div class="nav navbar-nav navbar-right" id="nav-login-section">
                        <a class="navbar-brand">Log in using</a>
                        <button type="submit" class="navbar-img" id="Facebook" name="provider" value="Facebook" title="Log in using your Facebook account"><img src="/images/logos/fb-white-50.png" /></button>
                        <button type="submit" class="navbar-img" id="Twitter" name="provider" value="Twitter" title="Log in using your Twitter account"><img src="/images/logos/twitter-white.png" /></button>
                    </div>
                }
            }
        </div>
    </div>
</nav>
